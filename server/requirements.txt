# Pin lower bounds only — lets pip pick wheels that match your Python version.
# Tested on Python 3.11–3.13; works on 3.14 once pydantic-core publishes a wheel.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.10
sse-starlette>=2.1

# Auth
sqlalchemy>=2.0
bcrypt>=4.2
python-jose[cryptography]>=3.3
email-validator>=2.2
python-dotenv>=1.0

# Postgres driver — used only when DATABASE_URL is set (else SQLite, no driver needed)
psycopg2-binary>=2.9

# Snowflake connector — used by /test-connection probes for sourceConfig.type='snowflake'
# (the upstream engine uses this driver inside stage scripts too)
snowflake-connector-python>=3.7

# BigQuery client — used by /test-connection probes for sourceConfig.type='bigquery'
# (upstream engine uses this driver inside stage scripts too)
google-cloud-bigquery>=3.20

# Google OAuth
authlib>=1.3
httpx>=0.27
itsdangerous>=2.2
